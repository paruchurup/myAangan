package com.myaangan.enums;
public enum PollStatus {
    DRAFT,    // Created, not yet visible to residents
    ACTIVE,   // Open for voting
    CLOSED,   // Voting ended, results visible
    ARCHIVED  // Hidden from listings
}
