package com.myaangan.enums;
public enum DocumentType {
    SOCIETY,      // Bylaws, rules, meeting minutes — visible to all
    NOC,          // Certificate issued to a specific resident
    MAINTENANCE   // Bills, receipts — linked to resident or society-wide
}
