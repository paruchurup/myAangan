package com.myaangan.enums;
public enum ResultVisibility {
    AFTER_VOTE,  // Voter sees results immediately after voting
    AFTER_CLOSE, // Results visible only after poll closes
    ADMIN_ONLY   // Only admin/creator sees results
}
