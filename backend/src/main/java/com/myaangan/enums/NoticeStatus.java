package com.myaangan.enums;
public enum NoticeStatus {
    DRAFT,      // Only visible to creator/admin
    PUBLISHED,  // Live — visible to targeted audience
    ARCHIVED    // Hidden from main feed
}
