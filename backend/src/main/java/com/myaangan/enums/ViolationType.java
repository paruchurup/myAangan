package com.myaangan.enums;
public enum ViolationType {
    WRONG_SLOT,       // Parked in wrong spot
    NO_STICKER,       // Missing society sticker
    BLOCKING,         // Blocking another vehicle / driveway
    DOUBLE_PARKED,    // Parked across two slots
    VISITOR_OVERSTAY, // Visitor parked beyond allowed time
    OTHER
}
